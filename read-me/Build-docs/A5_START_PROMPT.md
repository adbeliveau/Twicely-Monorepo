# A5 Seed Script — START

Read the attached file `TWICELY_V3_A5_SEED_PROMPT.md` completely before writing any code.

## Your task

Implement the A5 seed script exactly as specified in the prompt. 5 files, no more.

## Before you write ANY code

1. Read the full prompt
2. Query the existing `account` table to see Better Auth's password hash format: `SELECT password FROM account LIMIT 1;`
3. Check the existing schema files in `src/lib/db/schema/` to confirm table/column names match
4. List every file you will create or modify with a one-line description
5. Wait for my approval before proceeding

## Rules

- No `as any`, no `@ts-ignore`, no workarounds
- No files beyond the 5 listed in the prompt
- No modifications to existing files except `package.json`
- Show full terminal output for every command
- Explain every fix in plain English
- Save checkpoint tar BEFORE saying "done"

Go.
