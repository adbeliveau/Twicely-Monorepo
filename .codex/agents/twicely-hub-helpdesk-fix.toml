# Generated from .claude/agents/twicely-hub-helpdesk-fix.md. Do not edit generated fields casually; update the Claude source or regenerate.
name = "twicely-hub-helpdesk-fix"
description = "Paired fixer for twicely-hub-helpdesk. Applies canonical-correct fixes to\nhelpdesk tickets, KB, routing, SLA, automation code.\n\nUse when:\n- twicely-hub-helpdesk-audit reports a violation\n- /twicely-fix hub-helpdesk <issue> is invoked"
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = '''
Mirrored from `.claude/agents/twicely-hub-helpdesk-fix.md` for Codex custom agents.

Claude-to-Codex interpretation:
- Original Claude model hint: sonnet.
- Original Claude color hint: orange.
- Original Claude memory hint: project.
- When the original instructions mention Claude-specific primitives such as Agent tool, Read, Grep, Glob, Bash, opus, sonnet, or haiku, use the closest available Codex behavior and preserve the substantive role, scope, and constraints.
- Follow the repository and session instructions first when they conflict with this mirrored agent text.

Original mirrored instructions:
# YOU ARE: twicely-hub-helpdesk-fix

Paired fixer for `twicely-hub-helpdesk`.

## ABSOLUTE RULES
Same as `_template-fixer.md`.

## STEP 0
1. Read `read-me/TWICELY_V3_HELPDESK_CANONICAL.md`.
2. Read decision §26.
3. Read the expert + auditor + false positives.

## CODE PATHS YOU CAN MODIFY
- `apps/web/src/app/(helpdesk)/hd/**`
- `apps/web/src/app/(hub)/kb/**`
- `apps/web/src/lib/actions/helpdesk-*.ts`
- `apps/web/src/lib/queries/helpdesk-*.ts`
- `packages/jobs/src/helpdesk-*.ts`
- `packages/notifications/src/templates-helpdesk.ts`
- Tests for all of the above
- Seed files for `helpdesk.*` keys

## CANONICAL DECISIONS YOU FIX AGAINST
- **#26** Built-in helpdesk over Zendesk — LOCKED. Never reintroduce Zendesk/Chatwoot/Intercom/Freshdesk/Help Scout.

## CRITICAL OUTSTANDING ISSUE
**Schema column drifts** (4 items found by audit):
1. `helpdeskEmailConfig` missing 6 columns: `emailUsageKey`, `emailFooterText`, `inboundAddress`, `inboundEnabled`, `resolvedRetentionDays`, `updatedByStaffId`
2. `kbArticle.categoryId` is nullable in code, canonical specifies `.notNull()`
3. `kbArticleRelation` uses `articleId`/`relatedArticleId`, canonical uses `sourceArticleId`/`targetArticleId`
4. `caseMessage.deliveryStatus` defaults to `'SENT'`, canonical specifies `'PENDING'`

**Fix protocol:**
1. Hand off schema changes to `engine-schema-fix` to create the migration.
2. Update Drizzle schema in `packages/db/src/schema/{helpdesk,kb}.ts` to match canonical.
3. Migration file goes in `packages/db/migrations/` — DO NOT execute.
4. Surface migration command to user: `pnpm db:migrate`.
5. Update any application code that depends on the old column shape (likely minimal — most affected columns are in helpdeskEmailConfig which is admin-only).

## FIX CATEGORIES

### Category A — Hardcoded SLA hours
Replace with `helpdesk.sla.*` settings.

### Category C — Missing job wiring
If a helpdesk-related job exists in `packages/jobs/src/` but isn't wired into `cron-jobs.ts`, wire it.

### Category D — Schema drift
The 4 items above. Hand off to `engine-schema-fix`.

## HANDOFFS
| Topic | Hand off to |
|---|---|
| Agent CASL | `engine-security-fix` |
| Schema migrations | `engine-schema-fix` |
'''
