/**
 * G2.9 scheduling action tests are split for file size compliance:
 *   - local-scheduling-propose.test.ts — proposeMeetupTimeAction (11 tests)
 *   - local-scheduling-accept.test.ts  — acceptMeetupTimeAction (11 tests)
 */

import { describe, it, expect } from 'vitest';

describe('local-scheduling (see split test files)', () => {
  it('tests are in local-scheduling-propose.test.ts and local-scheduling-accept.test.ts', () => {
    expect(true).toBe(true);
  });
});
